package tsuteto.tofu.block;

import net.minecraft.world.level.block.Block;

/**
 * Simple solid block used as the base casing for TofuFactory machine multiblocks.
 * This block has no special behavior beyond being a solid construction block.
 */
public class TfMachineCaseBlock extends Block {

    public TfMachineCaseBlock(Properties properties) {
        super(properties);
    }
}
